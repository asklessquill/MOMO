# References Knowledge Capability — v0.5 Candidate

Status: **INACTIVE / NONCURRENT**

Owner under v0.5: **MOMO**

Pinned source: `asklessquill/references@88498df34eee902c72f9b0e8c53bf91996e49dab`

The existing `references` repository is reusable public knowledge/evidence. Under v0.5 it is a MOMO-owned knowledge capability, not a separate responsibility-bearing Actor.

Core rule:

> **Reference = evidence, not authority.**

MOMO may preserve reusable architecture, operations, controller-quality, failure, capability and cross-System lessons with provenance/status/currentness. Storage under MOMO responsibility does not turn a reference into Human constitutional authority or into an owning Application's current fact.

Historical AI-routing reference notes may remain useful evidence. Current model/tool routing responsibility belongs to INNU; an old or duplicated reference note does not by itself define INNU's current routing policy.

For recovery, a Fresh Actor must be able to learn from MOMO that reusable cross-System knowledge is a MOMO capability and that the old `references` location is provenance/storage, not a second owner.

The repository may remain technically separate while useful. Technical separation does not add an Actor. Later recovery/retirement verification determines which knowledge must also exist directly under an owner-side recovery path.

This candidate deletes or rewrites nothing and does not activate v0.5.
