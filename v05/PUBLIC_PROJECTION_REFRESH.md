# v0.5 Public Projection Refresh

Status: PRE-CLOSEOUT PREPARATION / DO NOT ALTER PHASE 8 TARGETS

The activated Current authority is `CURRENT.md`. Several public Human-facing surfaces still contain v0/A1-era wording and should be refreshed after closeout or after any Phase 8-required repair.

## Identified stale projections

1. `README.md`
   - still says MOMO-Observatory holds the Actor-readable semantic source.
   - v0.5 Current instead makes MOMO the accountable home for System meaning and treats Observatory as a MOMO-owned private evidence/recovery module.

2. `SOURCE_AND_VIEWS.md`
   - historical A1 projection explicitly calls MOMO-Observatory the AI-readable semantic source for MOMO System meaning.
   - preserve it as provenance, but add clear v0.5 historical/deprecated framing or replace the entry path so Fresh readers reach `CURRENT.md` first.

3. `site/data/current-state.json`
   - public cockpit still presents Observatory, FROG, SandFrog and Dango alongside the main component list and describes pre-v0.5 priorities.
   - v0.5 cockpit should show five Actors first, with modules nested under MOMO/INNU/SARU.

## Safe refresh rule

Do not modify the frozen Phase 8 exact targets before the Fresh Actor test. If Phase 8 PASSes despite these historical/public projections, refresh them as non-constitutional presentation work. If Phase 8 flags one as recovery-blocking, repair Current recovery paths first and repeat the bounded Fresh test.

Prepared candidate inputs:
- `v05/README_V05_CANDIDATE.md`
- `v05/PAGES_STATE.json`
- `v05/SYSTEM_REGISTRY.json`
